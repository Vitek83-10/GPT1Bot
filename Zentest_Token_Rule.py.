import asyncio
from pyrogram import Client

# 🔐 Токен твоего бота
BOT_TOKEN = "8085881327:AAHtgjesSjMbyektB5W2YXlSDQAGk_MMPfc"

# 🆔 Твой chat_id (куда бот должен отправить тестовое сообщение)
CHAT_ID = 1047298304

async def main():
    await asyncio.sleep(5)  # Подождём 5 секунд, чтобы бот успел стартовать
    async with Client("zentest_session", bot_token=BOT_TOKEN) as app:
        await app.send_message(
            chat_id=CHAT_ID,
            text="✅ Zentest_Token_Rule отработал — бот работает!"
        )

if __name__ == "__main__":
    asyncio.run(main())
